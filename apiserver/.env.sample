# Do NOT edit this file! Instead make a copy named ".env" and put your settings in there.
OC_BASEURL=https://oc.example.org/api
OC_AUTH_USER=changeme
OC_AUTH_PASS=changeme
# OIM specific parameters
BASEURL=https://localhost:9090/oimapi
SERVER_NAME=http://localhost
SERVER_PORT=9090
SPECDIR=openapi/
LOGGING_CONFIGFILE=logging.conf
# Make sure the logfile is in a volume or mounted path. Otherwise set it to /dev/null
LOGGING_LOGFILE=oim_error.log
LOGGING_MAILHOST=
